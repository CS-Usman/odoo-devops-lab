# Staging — http://<VM_IP>:8080/
# DB: postgres-staging pod on VM (not Azure Postgres)

fullnameOverride: odoo-staging

existingSecret: odoo-staging-db

database:
  host: postgres-staging

odoo:
  dbName: odoo_staging

persistence:
  hostPath: /srv/odoo/staging

ingress:
  entryPoint: webstaging
  port: 8080

resources:
  limits:
    memory: "1024Mi"
